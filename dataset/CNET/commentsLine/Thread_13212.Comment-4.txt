dynu.com provides free dynamic dns not only for third level(e.g.
*.dynu.com) but also top level(example.com) domain names.
dynu is actually one of the oldest dynamic dns service providers starting in 1997.
