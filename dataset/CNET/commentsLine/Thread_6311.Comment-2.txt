have been sensible choice, but given xp's base minimum requirements, it would actually make the old toshiba crawl instead of flying.
instead, i would just do a reload of winme to clear out all the old junks and make it useful as simple.. err.. web-browsing (if it is ever simple webpages).
