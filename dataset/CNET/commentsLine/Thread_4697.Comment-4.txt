there is no accepted method to copy drivers from an existing windows install.
we can clone (copy) a drive, we can copy files but drivers have more than files with registry entries and more.
its good you tried but why not clone the drive next time?
