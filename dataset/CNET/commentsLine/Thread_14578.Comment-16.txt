click start, right-click my computer, then click properties.
once in system properties click hardware, then click device manager.
once in device manager double-click the entry system devices.
if via drivers are installed in your system it should be listed there.
then you could download the latest hyperion drivers, otherwise if via drivers are not installed then other chipsets are installed.
swisse
