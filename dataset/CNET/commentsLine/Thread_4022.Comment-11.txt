(virtually)... the first thing we try is the ide to sata converter.
if that fails we install an ide pci card.
if that fails we try an external usb case.
at some point the owner makes the choice.
hope this helps.
bob
