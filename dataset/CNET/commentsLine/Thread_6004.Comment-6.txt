the easiest way to do it is to go to&gt; start orb&gt;run...&gt;type 'ipconfig /release'(without quotes) that should then reset the ip then you will need to go to the internet icon on the taskbar and right-click, then select 'diagnose and repair' there you have it, ip address reset
