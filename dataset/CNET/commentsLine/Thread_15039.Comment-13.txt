use your windows os install disk to delete all existing partitions, create as many paritions as you want (one partition using all the available space would be fine) and format.
don't complete the os installation, since you only need storage.
