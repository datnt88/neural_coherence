delete your display adapters in device manager before doing the install.
after rebooting, do not allow windows to reinstall the drivers.
then install the latest nvidia drivers -- that should fix what ails you.
