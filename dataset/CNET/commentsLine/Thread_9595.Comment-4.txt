the over a minute is too long [might not be].
you can edit the boot.ini file to add the switch &quot;/sos&quot; [no quotes], and then the boot screen will show each driver as it loads and some other posibly useful info.
can edit it using notepad.
