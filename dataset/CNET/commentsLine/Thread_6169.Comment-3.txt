run the network setup wizard on both machines.
turn on file and printer sharing on both.
give both the same network name.
on the last step on both machines, click the (something like) &quot;dont run on another machine, just finish&quot;.
