Sounds like hardware issue then.
Cable possibly but more than likely internal issue with scanner.
My suggestion would be to replace it with a newer USB input scanner, they aren't that expensive and are generally faster than the old SCSI hook ups.
