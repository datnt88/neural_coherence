the boot loader will be configured for ata/sata, and will not have the raid drivers at boottime (even if they're installed in the OS).
if raid-0 is enabled on the motherboard, it will erase the partition tables of the existing drives.
