If it's installed, I find no end of grief when the network isn't working.
You'll have to reveal if the DHCP address is valid.
If it's 192.168, then you can reveal the details here since that's a non-public IP... Bob
