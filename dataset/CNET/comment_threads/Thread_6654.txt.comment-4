It's called Dropbox.
It requires a manual upload, however, and doesn't propagate deletions.
Otherwise, research Webdav (shared storage on the web, accessible via Windows Explorer as a normal folder).
Kees
