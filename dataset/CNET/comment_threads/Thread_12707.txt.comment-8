Be nice if you could set the IP with a command line but windows says gui is better.
It is trivial to set the IP but it is so hard to describe in words.
It is in the tcpip properties for the wireless card.
There are a number of sites that show screen caps of how to set the IP if you search.
A wireless lan interface is configured exactly the same as any other.
Just google how to set a static ip.
Someone else hardcoded their IP is more than likely how they are interfering with you.
There is no way to know for sure the new IP you pick is not already assigned to another machine since it may be turned off.
That is why DHCP exists but you will eventually find on e that will work.
