To directly connect one computer to another, you must first set a static IP on each computer from the same subnet.
192.168.0.x x being different for each computer.
Once that's done, you should see each computer, but if you access through a router, you just need to leave your TCP/IP settings at obtain automatically.
