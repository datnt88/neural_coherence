That &quot;should&quot; ensure that a fresh installation will actually put the drivers onto the disk instead of just checking to see if the drivers have the same date.
Worth a try It is curious that both USB printers should stop though P
